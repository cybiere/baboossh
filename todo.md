# Testing follow-up

The initial test suite (`tests/`) covers `Db`, `Workspace` lifecycle, `Path` add/del/find, `Tag`, and `Extensions.load()`. Deliberately out of scope for that first pass — tracked here for later:

- **`shell/` command handlers** — closed as a lighter smoke pass (not `workspace/`-level exhaustiveness). Pure-DB dispatchers (`workspace`, `host`, `endpoint`, `user`, `creds`, `tag`, `path`, `scope`, `set`, `payload`, `connection list`, `store`/`exit`/`__eof`/`postcmd`) have full per-subcommand coverage in `tests/test_shell_*.py`, driven via cmd2's own `app.onecmd_plus_hooks(...)` harness (a new `shell` fixture in `tests/conftest.py`). SSH-dependent commands (`connect`, `run`, `probe`, `tunnel open`/`close`, `connection close`/`delete`) get smoke tests with mocked `Connection`/`Tunnel`, reusing the mocking approach from `test_workspace_connect_run.py`/`test_workspace_probe.py`/`test_workspace_tunnels.py` rather than re-testing branching logic already covered at the `Workspace` layer. `export`/`import` get light smoke tests against one real low-risk plugin each. 7 real bugs found and fixed along the way — see "`shell/` typing — closed" below.
- **`connection.py` (SSH/paramiko)** — partially covered now: `tests/test_docker_range.py` (opt-in, `--run-docker`) exercises `identify()`, `exec_command()` (via the `exec` payload), password auth, key auth, a real pivoted `direct-tcpip` connection, and the failure path against an incompatible SSH server, all against the live range in `tests/docker/`. Not covered: unit-level/mocked testing of `connection.py` in isolation (still needed for fast, Docker-less CI), `open_transport()`'s other gateway branches, tunnels.
- **`ext_dir/` extension plugins** — the `exec` payload is now covered end-to-end (`test_payload_exec_against_live_target`); `password`/`privkey` auth plugins are exercised incidentally via the same tests. The other payloads (`shell`, `getfile`, `putfile`, `gather`) and the import/export plugins are still untested; `test_extensions.py` still only covers plugin discovery/registration.
- **Remaining `Unique`-metaclass models** — `Host`, `Endpoint`, `User`, `Creds`, `Connection` don't have dedicated unit test files yet (CRUD/search methods aren't directly tested the way `Tag`'s are), though the Docker range tests do exercise `Endpoint`/`Host`/`User`/`Creds`/`Connection` incidentally through real usage.
- **`workspace/` mixins** — closed. All 11 mixin files (`hosts.py`, `endpoints.py`, `users.py`, `creds.py`, `options.py`, `connections.py`, `tags.py`, `paths.py`, `probe.py`, `scope.py`, `tunnels.py`) plus `Workspace`'s own `INIT`/`GETTERS` sections in `__init__.py` now have fast, Docker-less unit tests (`tests/test_workspace_*.py`, extended `tests/test_path.py`/`test_workspace.py`) covering every public method, including `probe`/`connect`/`run`/`tunnel_open`/`tunnel_close` via mocked `Connection`/`Tunnel` (not just the opt-in Docker range). 9 real bugs were found and fixed along the way — see the model-specific writeups below and the "Type hints follow-up" section.
- **CI matrix** — the workflow currently runs a single Python version (whatever `uv sync` resolves by default). Consider a version matrix once the suite is established, especially since `requires-python = ">=3.11"` claims broader support than is currently verified.

# Known bug: Connection.identify() stores shell error text as real field data

`Connection.identify()` (`baboossh/connection.py`) only checks the exit code of the `hostname` command; the exit codes of `uname -a`, `cat /etc/machine-id`, and the MAC-gathering pipeline are ignored, and their raw `result` string is stored regardless of success. On a host without `/etc/machine-id` (common on non-systemd distros), `cat /etc/machine-id` fails and its stderr text (e.g. `"cat: /etc/machine-id: No such file or directory"`) gets silently stored as `Host.machine_id`, rather than being treated as empty/absent. Doesn't crash `identify()` (found while designing a Docker test range covering non-systemd targets, where this would otherwise go unnoticed) but corrupts that field's meaning. Fix: check each command's exit code like `hostname` already does, and store `""`/`None` on failure instead of the error text.

# Known limitation: paramiko has no ChaCha20-Poly1305 support — baboossh cannot reach ChaCha20-only SSH servers

Confirmed empirically while building the Docker test range (`tests/docker/`): `paramiko` 5.0.0's cipher list (`paramiko.transport.Transport._preferred_ciphers`) is AES-CTR/AES-CBC/3DES/AES-GCM only — it does not implement `chacha20-poly1305@openssh.com` at all. TinySSH deliberately implements *only* that cipher (no AES, by design — see its man page), so there is zero cipher overlap and `baboossh` can never connect to a stock TinySSH server, or to any other SSH server restricted to ChaCha20-Poly1305 only. This isn't fixable via `baboossh`'s own config — it would need either a paramiko-side cipher implementation (out of this project's control) or switching SSH libraries, neither of which is proposed here. Tracked as a known target-compatibility gap; the test range deliberately includes a ChaCha20-only container (reachable, not hidden behind a pivot) specifically so `baboossh`'s failure mode against it is itself something to verify and, if needed, improve.

Confirmed via `tests/test_docker_range.py::test_incompatible_ssh_server_fails_cleanly`: the actual failure mode is an **uncaught** `paramiko.ssh_exception.IncompatiblePeer` propagating all the way out of `Workspace.probe()`. `Connection.probe()` (`baboossh/connection.py`) only catches `(TimeoutError, OSError, ConnectionRefusedError)` around `open_transport()`; a cipher/kex/host-key mismatch raises a `paramiko.SSHException` subclass instead, which isn't one of those. Neither `Workspace.probe()` nor the `do_probe`/`do_connect` shell commands catch it either, so in the interactive shell this currently surfaces as an unhandled traceback rather than the "OK"/"KO" style clean failure message the rest of `probe()` gives for a plain connection refusal or timeout. It's still a specific, clear exception (not a hang, not something generic) rather than the failure being silent — but arguably `probe()` should catch `paramiko.SSHException` alongside its existing catches and report it the same way. Not fixed here.

# Possible UX issue: Workspace.probe() is very slow against a genuinely unreachable host

Confirmed while exercising the Docker test range's fully-firewalled "unreachable" container: `probe()` against a host with no known/discoverable path took over 2 minutes before I killed it (it never finished). `probe()`'s fallback chain (`baboossh/workspace/probe.py`) tries several strategies in sequence — direct, given gateway, existing path, direct-from-local, then *every known `Host`* as a candidate gateway — and each attempt needs its own full connection timeout before falling through to the next. Against a target that's truly unreachable, every one of those attempts times out rather than failing fast, so the total wait scales with the number of known hosts. Not confirmed as a bug (no incorrect behavior, just slow), but worth a look — e.g. shorter timeouts for the exploratory fallback attempts, or surfacing progress/allowing cancellation, so `probe()` on an unreachable target doesn't feel like a hang.

# Type hints follow-up

`__all__` was added to every wildcard-imported module, and type hints were added to the 7 model classes (`Tag`, `Path`, `Endpoint`, `Host`, `User`, `Creds`, `Connection`) plus `Db`/`Extensions`/`Tunnel`/`utils.py`'s plain functions, and now the entire `baboossh/workspace/` and `baboossh/shell/` packages — both closed, see below. Deliberately out of scope and still open:

- **The `Unique` metaclass** (`utils.py`) — not typed. Metaclass typing (`__call__`/`__new__` interacting with per-workspace instance caching) is inherently advanced; `Self` usage in the classes that use it as their metaclass works fine without this. `pyright baboossh/` (full-package) currently reports 46 residual errors, all in `utils.py`'s `Unique` metaclass and `tunnel.py` (both pre-existing, untouched by the `workspace/`/`shell/` passes).
- **Full `mypy`/`pyright` CI enforcement** — hints were added and spot-checked file-by-file (`pyright` reports 0 errors on both `baboossh/workspace/` and `baboossh/shell/`; `mypy` isn't installed in this environment so only spot-checked historically), but no CI gate was wired up. Doing so now would still fail on the `Unique`/`tunnel.py` gap above and on genuinely unfixable noise (no type stubs exist for `paramiko`/`tabulate`).
- **CRUD base-class extraction** (the original audit finding this work was scoped down from) — still deferred, still gated on adding tests for `Host`/`Endpoint`/`User`/`Creds`/`Connection` first (see "Testing follow-up" above).

## `workspace/` typing — closed

All 11 mixin files now have full type hints, following the `Self`/native-union/`TYPE_CHECKING` conventions already established for the model classes, with one addition specific to this package: **mixin `self` parameters can't be annotated with the concrete `Workspace` class** (pyright rejects it — `Workspace` is a *subclass* of each mixin, not a supertype, and pyright requires an explicit `self` annotation to be a supertype of the enclosing class). Instead, each mixin file defines a small local `Protocol` under `TYPE_CHECKING` listing only the `Workspace` attributes/methods that file actually uses (`options`, `tunnels`, `workspace_folder`, `unstore`, `set_option`, `probe`, `path_del`, as needed), and annotates `self` with that Protocol — structural typing sidesteps the nominal-supertype rule cleanly. `pyright baboossh/workspace/` went from 56 errors to 0.

Adding tests first (before typing, per the plan this section originally set) surfaced **9 real, previously-unknown bugs**, all now fixed with regression tests in `tests/test_workspace_*.py`:
1. `hosts.py` `host_del` — missing `None`-check on `Host.find_one()` (defensive; practically unreachable given the membership check just above it).
2. `paths.py` `path_find_existing` — same missing-`None`-check pattern.
3. `paths.py` `path_find_existing` — `chain[0] = "local"` mutated a `list[Host | Endpoint | None]` with a `str`; refactored into a separate `labels: list[str]` built via list comprehension, output unchanged.
4. `probe.py` `probe()` — an unknown/typo'd gateway host name crashed with `AttributeError` instead of a clean error; now prints and returns cleanly.
5. `probe.py` `probe()`'s `host` variable — pyright flagged it "possibly unbound"; traced by hand and confirmed not a live bug (a prior unconditional assignment always covers every path that reaches the use site) — added a no-op `host: "Host | None" = None` initializer purely to satisfy the type checker.
6. `tunnels.py` `tunnel_open` — `Connection.from_target()` returning `None` was passed straight into `Tunnel()`, which requires a non-`None` connection; now checked and reported cleanly.
7. `tunnels.py` `tunnel_close` — **the one real, reachable bug of the batch**: printed "No tunnel on port X" but then fell through to `self.tunnels.pop(port)` with no `default=`, raising an uncaught `KeyError` right after the "clean" error message. Missing `return`. Caught by a test crashing at workspace-fixture teardown before the fix.
8. `options.py` `set_option` — `set_option("connection", None)` cleared and printed the three connection options but fell through (missing `return`) into `Connection.from_target(None)`, crashing with `TypeError`. Caught immediately on first test run.
9. `connections.py` `enum_run` — the `!tagname` branch built an unused `endpoints = tag.endpoints` (plural) instead of assigning the `Tag` object to the `endpoint` variable actually passed to `Connection.find_all(endpoint=endpoint, ...)`, which accepts `Endpoint | Tag | None`; a target like `"user:cred@!tagname"` left `endpoint` as the raw string `"!tagname"` instead. Found while widening the variable's type annotation during the typing pass (not by pyright directly, and not part of the original 56-error baseline).

## `shell/` typing — closed

All 16 command files (`workspace.py`, `hosts.py`, `endpoints.py`, `users.py`, `creds.py`, `payloads.py`, `connections.py`, `options.py`, `tags.py`, `paths.py`, `probe.py`, `connect.py`, `tunnels.py`, `exports.py`, `imports.py`, `scope.py`) plus `helpers.py` (17, not 16 as previously miscounted, `get_*` completion-provider functions) and `Shell` itself in `__init__.py` are now fully typed. `pyright baboossh/shell/` went from 66 errors to 0.

One deviation from the `workspace/` package's `Protocol`-on-`self` pattern: `cmd2.with_argparser`'s decorator type signature requires `self: cmd2.Cmd | cmd2.CommandSet` (nominal, not structural) — a `Protocol` doesn't satisfy it. So every `do_*` dispatcher method's `self` is left untyped (with a `# pyright: ignore[reportArgumentType]` on its `@with_argparser(...)` line, since pyright still flags the decorator call itself), while each file's *private* `set_defaults`-target handler methods (e.g. `__host_list`) are typed normally via the same per-file local `Protocol` pattern as `workspace/`. Wherever an untyped `do_*` needs to call a typed private handler or `self.workspace` directly, the specific line gets a matching targeted `# pyright: ignore[reportAttributeAccessIssue]` — all documented inline as "self isn't _Shell on a mixin, see plan".

Writing tests (a `shell` fixture in `tests/conftest.py`, driving commands via `app.onecmd_plus_hooks(...)`) surfaced **7 real, previously-unknown bugs**, all now fixed with regression/smoke tests in `tests/test_shell_*.py`:
1. `options.py` `do_set` — same "possibly-unbound value if `option` matches none of the expected branches" shape as `workspace/`'s bug 8; practically unreachable given `set_defaults` guarantees, fixed defensively with an `else:` branch.
2. `__init__.py` `postcmd` — untyped override with its 3rd param renamed from cmd2's `statement` to `line`; typed and renamed back to match the base exactly, zero behavior change.
3. **Severe, real, previously-unknown**: `baboossh/shell/__init__.py` called `Extensions.load()` *after* importing `creds.py`/`exports.py`/`imports.py`, but those files build their per-type subparsers (`creds add <type>`, `export <type>`, `import <type>`) from `Extensions.auths`/`exports`/`imports` **at class-body (import) time** — those dicts were still empty, so every one of those subcommands has *always* failed with `Error: invalid choice: ... (choose from )`. Confirmed pre-existing via `git stash`. Fixed by moving `Extensions.load()` before the `shell.*` imports.
4. `connections.py` `connection close` (no argument) — the `connection` arg uses `nargs="?"` (optional), so omitting it passed `None` straight into `Connection.from_target(None)`, crashing with `TypeError` (same shape as bug 8 below). Fixed with an explicit `None`-check.
5. **Severe, real, previously-unknown**: `connect.py` `do_run` unconditionally called `self._reset_completion_defaults()`, a private cmd2 method that **does not exist** on the pinned `cmd2` 4.1.2 — likely a leftover from an older cmd2 internal API. Every invocation of `run` crashed immediately with `AttributeError`, regardless of arguments. Confirmed by direct execution. Fixed by removing the dead call (not load-bearing for `do_run`'s actual logic).
6. Found immediately after fixing bug 5, in the same function: `do_run`'s fallback-to-`options["payload"]` path called `payload.buildParser(__parser)` *before* checking whether `payload` was `None` — so `run` with no subcommand and no active `set payload` crashed with `AttributeError: 'NoneType' object has no attribute 'buildParser'` instead of the intended "Error : No payload specified" message. Fixed by moving the `None`-check earlier.
7. `__init__.py` `do__eof` — overrides `cmd2.Cmd.do__eof`, which cmd2 itself decorates with `@with_argparser` (declared param type `argparse.Namespace`); baboossh's override is deliberately undecorated (matching the adjacent `do_exit`/`do_store` — EOF needs no arguments), so it actually receives a raw `str` at runtime. Not a bug — an intentional, pre-existing design choice — but genuinely incompatible from pyright's perspective, since it can't see through the decorator. Annotated as `_: str -> bool` and suppressed with a documented `# pyright: ignore[reportIncompatibleMethodOverride]`.

Also worth noting: resolving `Workspace.connection_close`/`connection_del`'s types from `baboossh/shell/connections.py` triggers a confusing but harmless cross-package pyright artifact (nested same-named local `_Workspace` Protocols across different `baboossh/workspace/*.py` files resolving through each other when accessed externally — `pyright baboossh/workspace/` alone is unaffected, 0 errors). Suppressed with two targeted, documented ignores rather than restructuring the `workspace/` package's per-file Protocol naming.
